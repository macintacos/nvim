-- Local plugin (no upstream repo)
-- Tweaks to the built-in vim.pack UI; currently: press <CR> in the update
-- confirmation buffer to open a commit/tag in its remote (GitHub).
require("plugins.pack-tweaks").setup({})
